spring:
  datasource:
    url: jdbc:h2:mem:digital_banking_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: false

app:
  security:
    jwt:
      secret: test-secret-test-secret-test-secret-test-secret
      expiration-ms: 86400000
  ai:
    openai:
      api-key:
      model: gpt-5.2
  telegram:
    bot-token:
    webhook-secret:
